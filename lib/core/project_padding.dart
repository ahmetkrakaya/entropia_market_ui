import 'package:flutter/material.dart';

mixin ProjectPadding{
  final generalPadding = const EdgeInsets.all(12);
  final leftPadding = const EdgeInsets.only(left: 12);
  final rightPadding = const EdgeInsets.only(right: 12);
  final topPadding = const EdgeInsets.only(top: 12);
  final bottomPadding = const EdgeInsets.only(bottom: 12);
  final horizontalPadding = const EdgeInsets.symmetric(horizontal: 12);
  final verticalPadding = const EdgeInsets.symmetric(vertical: 12);

}